# FourMeme Creator Analyzer Chrome 插件 PRD
## 公开版本（无需 API Key）

> 产品需求文档 (Product Requirements Document)
> 版本：2.0 Public Edition
> 创建日期：2025-01-12
> 状态：技术可行性已验证

---

## 目录

1. [产品概述](#1-产品概述)
2. [公开 API 能力总览](#2-公开-api-能力总览)
3. [可实现的功能清单](#3-可实现的功能清单)
4. [功能详细规格](#4-功能详细规格)
5. [技术架构](#5-技术架构)
6. [UI/UX 设计](#6-uiux-设计)
7. [开发规范](#7-开发规范)
8. [限制与风险](#8-限制与风险)
9. [版本规划](#9-版本规划)

---

## 1. 产品概述

### 1.1 产品定位

**公开版本**的 FourMeme Creator Analyzer 是一款完全免费、无需用户配置任何 API Key 的 Chrome 浏览器插件。用户安装即用，零配置门槛。

### 1.2 核心特点

| 特点 | 说明 |
|-----|------|
| **零配置** | 安装即用，无需注册或配置 API Key |
| **完全免费** | 所有功能免费使用 |
| **数据全面** | 利用多个公开 API 整合数据 |
| **实时性好** | 直接调用官方和第三方公开接口 |

---

## 2. 公开 API 能力总览

### 2.1 已验证可用的公开 API

| API | 免费额度 | 需要 Key | 主要能力 |
|-----|---------|---------|---------|
| **Four.meme API** | 无限制 | ❌ | Creator 地址、代币信息、持有者列表 |
| **DexScreener API** | 300次/分钟 | ❌ | 价格、流动性、交易量、买卖数据 |
| **GeckoTerminal API** | 无限制 | ❌ | 代币信息、流动性池、市场数据 |
| **BSC 公共 RPC** | 无限制 | ❌ | 链上余额查询、合约调用 |

### 2.2 Four.meme 官方 API（核心数据源）

#### 已验证可用端点

| 端点 | 方法 | 返回数据 | 状态 |
|-----|------|---------|------|
| `/private/token/get?address={addr}` | GET | 代币详情 + Creator 地址 | ✅ 已验证 |
| `/private/token/holder?address={addr}&pageNo=1&pageSize=100` | GET | 前100名持有者列表 | ✅ 已验证 |

#### 代币详情 API 返回字段

```json
{
  "code": 0,
  "data": {
    // ⭐ Creator 信息（核心）
    "userId": 107798996,
    "userAddress": "0x03d06b6b44b1996944773ad94b229bb3e2a66a10",
    "userName": "0x03d06b6b44b1996944773ad94b229bb3e2a66a10",

    // 代币基础信息
    "address": "0x226f17fdea2578f65ac799b0f40f599cf61c4444",
    "name": "推特钱包代币",
    "symbol": "C",
    "shortName": "C",
    "imageUrl": "https://...",
    "description": "...",

    // 供应量
    "totalAmount": "1000000000000000000000000000",
    "saleAmount": "800000000000000000000000000",

    // 市场数据
    "marketCap": 65486,
    "tokenPrice": 0.000065486,
    "dayIncrease": -17.69,

    // 交易状态
    "status": "TRADE",           // INIT/TRADE/STOP
    "dexType": "PANCAKE_SWAP",
    "tradeUrl": "https://...",

    // 时间
    "launchTime": 1736672100000,
    "createDate": "2025-01-12",

    // 社交链接
    "twitter": "...",
    "telegram": "...",
    "website": "..."
  }
}
```

#### 持有者列表 API 返回字段

```json
{
  "code": 0,
  "data": [
    {
      "address": "0x...",        // 持有者地址
      "amount": "213000000...",  // 持有数量（wei）
      "userName": "0x..."        // 用户名
    },
    // ... 最多100个持有者
  ]
}
```

### 2.3 DexScreener API

**基础 URL**: `https://api.dexscreener.com`

| 端点 | 返回数据 |
|-----|---------|
| `/latest/dex/tokens/{address}` | 代币所有交易对 |
| `/latest/dex/pairs/{chain}/{pairAddress}` | 单个交易对详情 |
| `/latest/dex/search?q={query}` | 搜索代币/交易对 |

**返回数据字段**：
- 价格（USD、本币）
- 24h/1h/5m 价格变化
- 流动性
- 市值/FDV
- 24h 交易量
- 买卖笔数（buys/sells）
- 交易对地址
- DEX 名称

### 2.4 GeckoTerminal API

**基础 URL**: `https://api.geckoterminal.com/api/v2`

| 端点 | 返回数据 |
|-----|---------|
| `/networks/bsc/tokens/{address}` | 代币基础信息 |
| `/networks/bsc/tokens/{address}/pools` | 关联流动性池 |
| `/networks/bsc/pools/{poolAddress}` | 流动性池详情 |

**返回数据字段**：
- 代币名称、符号、精度
- 总供应量
- FDV
- 24h 交易量
- 买卖笔数、买家/卖家数量
- 储备金
- 价格变化

### 2.5 BSC 公共 RPC

**公共节点**:
- `https://bsc-dataseed.bnbchain.org`
- `https://bsc-dataseed1.binance.org`
- `https://rpc.ankr.com/bsc` (Ankr 免费)

**可用方法**：
- `eth_call` - 调用合约只读函数
- `eth_getBalance` - 获取 BNB 余额
- `eth_blockNumber` - 获取最新区块

**代币余额查询示例**：
```javascript
// 调用 ERC20 balanceOf 函数
const data = ethers.utils.encodeFunctionData('balanceOf', [walletAddress]);
const result = await provider.call({
  to: tokenAddress,
  data: data
});
```

---

## 3. 可实现的功能清单

### 3.1 功能矩阵

| 功能 | 可行性 | 数据源 | 优先级 |
|-----|-------|-------|-------|
| **自动识别代币地址** | ✅ 完全可行 | URL 解析 | P0 |
| **获取 Creator 地址** | ✅ 完全可行 | Four.meme API | P0 |
| **Creator 当前持仓** | ✅ 完全可行 | Four.meme Holder API | P0 |
| **代币基础信息** | ✅ 完全可行 | Four.meme API | P0 |
| **持有者分布分析** | ✅ 完全可行 | Four.meme Holder API | P1 |
| **价格/市值数据** | ✅ 完全可行 | DexScreener + GeckoTerminal | P1 |
| **流动性分析** | ✅ 完全可行 | DexScreener + GeckoTerminal | P1 |
| **24h 买卖数据** | ✅ 完全可行 | DexScreener + GeckoTerminal | P1 |
| **Creator 余额** | ✅ 完全可行 | BSC RPC eth_call | P1 |
| **风险评分（简化版）** | ✅ 完全可行 | 本地计算 | P1 |
| **社交链接展示** | ✅ 完全可行 | Four.meme API | P2 |
| ~~Creator 历史代币~~ | ❌ 不可行 | 需要 Bitquery | - |
| ~~Creator 交易历史~~ | ❌ 不可行 | 需要 BSCScan Key | - |
| ~~关联地址分析~~ | ❌ 不可行 | 需要付费 API | - |
| ~~实时预警推送~~ | ⚠️ 受限 | 需要定时轮询 | P3 |

### 3.2 功能分组

```
┌─────────────────────────────────────────────────────────────────┐
│                    公开版本功能架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              第一层：核心功能（P0）                        │   │
│  │                                                          │   │
│  │  ✅ 自动识别代币地址                                      │   │
│  │  ✅ 获取 Creator 钱包地址                                 │   │
│  │  ✅ Creator 在该代币的持仓量和占比                        │   │
│  │  ✅ 代币基础信息（名称、符号、总量等）                     │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              第二层：分析功能（P1）                        │   │
│  │                                                          │   │
│  │  ✅ 持有者集中度分析（前100名分布）                       │   │
│  │  ✅ Creator 持仓排名                                      │   │
│  │  ✅ 实时价格 / 市值 / FDV                                 │   │
│  │  ✅ 流动性深度                                            │   │
│  │  ✅ 24h 交易量 / 买卖笔数                                 │   │
│  │  ✅ Creator BNB 余额                                      │   │
│  │  ✅ 简化版风险评分                                        │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              第三层：展示功能（P2）                        │   │
│  │                                                          │   │
│  │  ✅ 社交链接（Twitter/Telegram/Website）                  │   │
│  │  ✅ 交易链接（跳转 PancakeSwap）                          │   │
│  │  ✅ 持有者地址列表（可复制）                              │   │
│  │  ✅ 多数据源价格对比                                      │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 功能详细规格

### 4.1 模块一：自动识别代币地址（P0）

**实现方式**：解析当前页面 URL

```javascript
// URL 模式：https://four.meme/{locale}/token/{address}
const URL_PATTERN = /^https:\/\/four\.meme\/[\w-]*\/?token\/(0x[a-fA-F0-9]{40})/;

function extractTokenAddress(url) {
  const match = url.match(URL_PATTERN);
  return match ? match[1] : null;
}
```

**触发时机**：
- 用户点击插件图标时
- 页面 URL 变化时（SPA 路由监听）

---

### 4.2 模块二：Creator 信息获取（P0）

**数据获取流程**：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  代币地址    │ ──▶ │ Four.meme   │ ──▶ │  Creator    │
│  (URL解析)  │     │ Token API   │     │  地址       │
└─────────────┘     └─────────────┘     └─────────────┘
```

**API 调用**：
```javascript
async function getCreatorAddress(tokenAddress) {
  const response = await fetch(
    `https://four.meme/meme-api/v1/private/token/get?address=${tokenAddress}`
  );
  const data = await response.json();
  return {
    creatorAddress: data.data.userAddress,
    creatorId: data.data.userId,
    creatorName: data.data.userName
  };
}
```

---

### 4.3 模块三：Creator 持仓分析（P0）

**数据获取**：
```javascript
async function getCreatorHolding(tokenAddress, creatorAddress) {
  // 1. 获取持有者列表
  const response = await fetch(
    `https://four.meme/meme-api/v1/private/token/holder?address=${tokenAddress}&pageNo=1&pageSize=100`
  );
  const holders = await response.json();

  // 2. 在持有者中查找 Creator
  const creatorHolding = holders.data.find(
    h => h.address.toLowerCase() === creatorAddress.toLowerCase()
  );

  // 3. 计算持仓占比
  const totalSupply = await getTotalSupply(tokenAddress);
  const holdingPercent = (creatorHolding.amount / totalSupply) * 100;

  // 4. 计算排名
  const rank = holders.data.findIndex(
    h => h.address.toLowerCase() === creatorAddress.toLowerCase()
  ) + 1;

  return {
    amount: creatorHolding?.amount || 0,
    percent: holdingPercent,
    rank: rank || 'Not in Top 100'
  };
}
```

**展示数据**：
| 字段 | 说明 |
|-----|------|
| 持仓数量 | Creator 持有的代币数量 |
| 持仓占比 | 占总供应量的百分比 |
| 持仓排名 | 在 Top 100 中的排名 |

---

### 4.4 模块四：持有者集中度分析（P1）

**分析维度**：

```javascript
function analyzeHolderConcentration(holders, totalSupply) {
  // Top 10 持仓占比
  const top10Percent = holders.slice(0, 10)
    .reduce((sum, h) => sum + Number(h.amount), 0) / totalSupply * 100;

  // Top 20 持仓占比
  const top20Percent = holders.slice(0, 20)
    .reduce((sum, h) => sum + Number(h.amount), 0) / totalSupply * 100;

  // 最大持仓者占比
  const largestHolderPercent = Number(holders[0].amount) / totalSupply * 100;

  // 计算基尼系数（简化版）
  const giniCoefficient = calculateGini(holders.map(h => Number(h.amount)));

  return {
    top10Percent,
    top20Percent,
    largestHolderPercent,
    giniCoefficient,
    holderCount: holders.length,
    riskLevel: top10Percent > 80 ? 'HIGH' : top10Percent > 50 ? 'MEDIUM' : 'LOW'
  };
}
```

**风险等级判定**：
| Top 10 占比 | 风险等级 | 说明 |
|------------|---------|------|
| > 80% | 🔴 高风险 | 极度集中，容易被操控 |
| 50-80% | 🟡 中风险 | 较为集中，需关注 |
| < 50% | 🟢 低风险 | 分布较均匀 |

---

### 4.5 模块五：市场数据整合（P1）

**多数据源整合**：

```javascript
async function getMarketData(tokenAddress) {
  // 并行请求多个数据源
  const [dexScreener, geckoTerminal, fourmeme] = await Promise.all([
    fetchDexScreener(tokenAddress),
    fetchGeckoTerminal(tokenAddress),
    fetchFourmeme(tokenAddress)
  ]);

  return {
    // 价格（取多源平均或显示多个）
    price: {
      dexScreener: dexScreener.priceUsd,
      geckoTerminal: geckoTerminal.price_usd,
      fourmeme: fourmeme.tokenPrice
    },

    // 市值
    marketCap: dexScreener.marketCap || geckoTerminal.market_cap_usd,

    // FDV
    fdv: dexScreener.fdv || geckoTerminal.fdv_usd,

    // 流动性
    liquidity: dexScreener.liquidity?.usd || geckoTerminal.reserve_in_usd,

    // 24h 交易量
    volume24h: dexScreener.volume?.h24 || geckoTerminal.volume_usd?.h24,

    // 价格变化
    priceChange: {
      h1: dexScreener.priceChange?.h1,
      h24: dexScreener.priceChange?.h24 || fourmeme.dayIncrease
    },

    // 买卖数据
    txns24h: {
      buys: dexScreener.txns?.h24?.buys,
      sells: dexScreener.txns?.h24?.sells
    }
  };
}
```

---

### 4.6 模块六：Creator BNB 余额查询（P1）

**通过公共 RPC 查询**：

```javascript
import { ethers } from 'ethers';

const BSC_RPC = 'https://bsc-dataseed.bnbchain.org';

async function getCreatorBNBBalance(creatorAddress) {
  const provider = new ethers.JsonRpcProvider(BSC_RPC);
  const balance = await provider.getBalance(creatorAddress);
  return ethers.formatEther(balance);
}
```

---

### 4.7 模块七：简化版风险评分（P1）

**评分模型（基于公开数据）**：

```
┌─────────────────────────────────────────────────────────────────┐
│                    简化版风险评分模型                             │
│                    总分 100 分（分数越高越安全）                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度一：Creator 持仓 (35分)                               │  │
│  │   - 持仓 < 5%:    +35分 (非常好)                          │  │
│  │   - 持仓 5-10%:   +25分 (良好)                            │  │
│  │   - 持仓 10-20%:  +15分 (一般)                            │  │
│  │   - 持仓 20-50%:  +5分  (较高)                            │  │
│  │   - 持仓 > 50%:   +0分  (风险)                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度二：持有者集中度 (25分)                               │  │
│  │   - Top10 < 50%:  +25分                                   │  │
│  │   - Top10 50-70%: +15分                                   │  │
│  │   - Top10 70-90%: +5分                                    │  │
│  │   - Top10 > 90%:  +0分                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度三：流动性 (20分)                                     │  │
│  │   - 流动性 > $50k:  +20分                                 │  │
│  │   - 流动性 $20-50k: +15分                                 │  │
│  │   - 流动性 $10-20k: +10分                                 │  │
│  │   - 流动性 $5-10k:  +5分                                  │  │
│  │   - 流动性 < $5k:   +0分                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度四：交易活跃度 (10分)                                 │  │
│  │   - 24h 交易 > 1000笔: +10分                              │  │
│  │   - 24h 交易 500-1000: +7分                               │  │
│  │   - 24h 交易 100-500:  +5分                               │  │
│  │   - 24h 交易 < 100:    +2分                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度五：Creator BNB 余额 (10分)                           │  │
│  │   - 余额 > 10 BNB: +10分                                  │  │
│  │   - 余额 1-10 BNB: +7分                                   │  │
│  │   - 余额 0.1-1 BNB: +4分                                  │  │
│  │   - 余额 < 0.1 BNB: +0分                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**风险等级**：
| 分数 | 等级 | 颜色 | 建议 |
|-----|------|------|------|
| 80-100 | 低风险 | 🟢 | 相对安全 |
| 60-79 | 中低风险 | 🟡 | 需关注 |
| 40-59 | 中高风险 | 🟠 | 谨慎 |
| 20-39 | 高风险 | 🔴 | 不建议 |
| 0-19 | 极高风险 | ⚫ | 远离 |

---

## 5. 技术架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Chrome Extension (Manifest V3)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Popup UI (React)                      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ Creator │ │ Holders │ │ Market  │ │  Risk   │       │   │
│  │  │  Info   │ │ Analysis│ │  Data   │ │  Score  │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  Service Worker                          │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │   │
│  │  │ API 聚合器 │  │ 缓存管理器 │  │ 评分计算器 │           │   │
│  │  └───────────┘  └───────────┘  └───────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Content Script                        │   │
│  │         (URL 监听 / 页面数据提取)                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    公开 API 层（无需 Key）                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Four.meme   │  │ DexScreener │  │GeckoTerminal│             │
│  │    API      │  │    API      │  │    API      │             │
│  │             │  │             │  │             │             │
│  │ • Token     │  │ • Price     │  │ • Token     │             │
│  │ • Holders   │  │ • Liquidity │  │ • Pools     │             │
│  │ • Creator   │  │ • Volume    │  │ • Volume    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────────────────────────────────────────┐           │
│  │              BSC 公共 RPC                        │           │
│  │   https://bsc-dataseed.bnbchain.org             │           │
│  │   • eth_getBalance (BNB 余额)                   │           │
│  │   • eth_call (代币余额)                          │           │
│  └─────────────────────────────────────────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 技术栈

| 层级 | 技术 | 说明 |
|-----|------|------|
| UI 框架 | React 18 + TypeScript | 类型安全、组件化 |
| 状态管理 | Zustand | 轻量级 |
| 样式 | Tailwind CSS | 快速开发 |
| 构建 | Vite + CRXJS | Manifest V3 支持 |
| Web3 | ethers.js v6 | BSC RPC 调用 |
| HTTP | fetch API | 原生，无依赖 |

### 5.3 Manifest V3 配置

```json
{
  "manifest_version": 3,
  "name": "FourMeme Creator Analyzer",
  "version": "1.0.0",
  "description": "一键分析 Four.meme 代币创建者数据，无需配置",

  "permissions": [
    "activeTab",
    "storage"
  ],

  "host_permissions": [
    "https://four.meme/*",
    "https://api.dexscreener.com/*",
    "https://api.geckoterminal.com/*",
    "https://bsc-dataseed.bnbchain.org/*",
    "https://bsc-dataseed1.binance.org/*"
  ],

  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },

  "background": {
    "service_worker": "background.js",
    "type": "module"
  },

  "content_scripts": [
    {
      "matches": ["https://four.meme/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
```

### 5.4 目录结构

```
fourmeme-analyzer/
├── src/
│   ├── popup/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── CreatorCard.tsx      # Creator 信息卡片
│   │   │   ├── HoldingAnalysis.tsx  # 持仓分析
│   │   │   ├── HolderChart.tsx      # 持有者分布图
│   │   │   ├── MarketData.tsx       # 市场数据
│   │   │   ├── RiskScore.tsx        # 风险评分
│   │   │   └── SocialLinks.tsx      # 社交链接
│   │   └── hooks/
│   │       ├── useTokenData.ts      # 代币数据 Hook
│   │       └── useCreatorData.ts    # Creator 数据 Hook
│   │
│   ├── background/
│   │   ├── index.ts
│   │   └── api/
│   │       ├── fourmeme.ts          # Four.meme API
│   │       ├── dexscreener.ts       # DexScreener API
│   │       ├── geckoterminal.ts     # GeckoTerminal API
│   │       └── bsc-rpc.ts           # BSC RPC 调用
│   │
│   ├── content/
│   │   └── index.ts                 # URL 监听
│   │
│   ├── services/
│   │   ├── riskScore.ts             # 风险评分计算
│   │   ├── holderAnalysis.ts        # 持有者分析
│   │   └── cache.ts                 # 缓存服务
│   │
│   └── types/
│       └── index.ts                 # 类型定义
│
├── public/
│   └── manifest.json
│
└── package.json
```

---

## 6. UI/UX 设计

### 6.1 Popup 界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  🔍 FourMeme Analyzer                                    [⚙️]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  📦 代币信息                                               │ │
│  │  名称: 推特钱包代币 (C)                                    │ │
│  │  合约: 0x226f...4444                              [📋]    │ │
│  │  状态: 🟢 已上 DEX (PancakeSwap)                          │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  👤 Creator 信息                                          │ │
│  │  ──────────────────────────────────────────────────────── │ │
│  │  地址: 0x03d0...a10                               [📋]    │ │
│  │  BNB 余额: 2.35 BNB                                       │ │
│  │                                                           │ │
│  │  📊 持仓分析                                              │ │
│  │  持有量: 50,000,000 C                                     │ │
│  │  占比: 5.0%                                               │ │
│  │  排名: #3 / Top 100                                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  ⚠️ 风险评分                                              │ │
│  │  ──────────────────────────────────────────────────────── │ │
│  │                                                           │ │
│  │     🟢 72 / 100  中低风险                                  │ │
│  │     ████████████████████░░░░░░░░                          │ │
│  │                                                           │ │
│  │  ✅ Creator 持仓 5%     (+25)                              │ │
│  │  ✅ Top10 占比 45%      (+25)                              │ │
│  │  ✅ 流动性 $32k         (+15)                              │ │
│  │  ✅ 24h 交易 4,200笔    (+10)                              │ │
│  │  ⚠️ Creator BNB 2.35    (+4)                              │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  📈 市场数据                                              │ │
│  │  ──────────────────────────────────────────────────────── │ │
│  │  价格: $0.0000765                     24h: 🔴 -17.69%     │ │
│  │  市值: $76,500                        FDV: $76,500        │ │
│  │  流动性: $32,358                      24h量: $504,230     │ │
│  │  买入: 2,485笔                        卖出: 2,599笔       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  👥 持有者分布                                            │ │
│  │  ──────────────────────────────────────────────────────── │ │
│  │  Top 10 占比: 45.2%  🟢 分布良好                          │ │
│  │  Top 20 占比: 58.7%                                       │ │
│  │  最大持有: 21.3% (0x1234...5678)                          │ │
│  │                                                           │ │
│  │  [查看 Top 100 持有者列表 ▼]                              │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                         │
│  │ 🐦 X    │ │ ✈️ TG   │ │ 🌐 Web  │                         │
│  └─────────┘ └─────────┘ └─────────┘                         │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  [🥞 在 PancakeSwap 交易]                                 │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 颜色规范

| 用途 | 颜色 | HEX |
|-----|------|-----|
| 主色调 | 紫色 | #8B5CF6 |
| 背景 | 深灰 | #0F172A |
| 卡片 | 灰色 | #1E293B |
| 文字 | 白色 | #F8FAFC |
| 次要文字 | 灰色 | #94A3B8 |
| 成功/低风险 | 绿色 | #22C55E |
| 警告/中风险 | 黄色 | #EAB308 |
| 危险/高风险 | 红色 | #EF4444 |

---

## 7. 开发规范

### 7.1 API 调用规范

```typescript
// API 统一封装
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
}

async function fetchWithFallback<T>(
  primaryUrl: string,
  fallbackUrl?: string
): Promise<ApiResponse<T>> {
  try {
    const response = await fetch(primaryUrl);
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    const data = await response.json();
    return { success: true, data };
  } catch (error) {
    if (fallbackUrl) {
      return fetchWithFallback(fallbackUrl);
    }
    return { success: false, error: error.message };
  }
}
```

### 7.2 缓存策略

| 数据类型 | 缓存时间 | 存储 |
|---------|---------|------|
| Creator 地址 | 1 小时 | chrome.storage.local |
| 持有者列表 | 2 分钟 | 内存 |
| 市场数据 | 30 秒 | 内存 |
| 风险评分 | 5 分钟 | chrome.storage.local |

### 7.3 错误处理

```typescript
// 统一错误处理
function handleApiError(error: Error, context: string) {
  console.error(`[${context}] Error:`, error.message);

  // 返回用户友好的错误信息
  return {
    message: getErrorMessage(error),
    retryable: isRetryableError(error)
  };
}
```

---

## 8. 限制与风险

### 8.1 功能限制

| 限制项 | 原因 | 影响 |
|-------|------|------|
| 无法获取 Creator 历史代币 | 需要 Bitquery 等付费 API | 无法追踪 Creator 过往记录 |
| 无法获取交易历史 | 需要 BSCScan API Key | 无法分析 Creator 买卖行为 |
| 无法检测关联地址 | 需要深度链上分析 | 可能漏检分散持仓 |
| 持有者仅前 100 名 | Four.meme API 限制 | 无法获取完整持有者列表 |

### 8.2 数据风险

| 风险 | 说明 | 缓解措施 |
|-----|------|---------|
| API 变更 | Four.meme 未公开文档 | 监控 API 响应，及时更新 |
| 数据延迟 | 多源数据时间差 | 显示数据更新时间 |
| 价格不一致 | 不同 DEX 价格差异 | 显示多源价格对比 |

### 8.3 免责声明

> 本插件提供的数据仅供参考，不构成投资建议。用户应自行判断投资风险，本插件不对任何投资损失负责。

---

## 9. 版本规划

### 9.1 v1.0 MVP

| 功能 | 状态 |
|-----|------|
| 自动识别代币地址 | 待开发 |
| 获取 Creator 地址 | 待开发 |
| Creator 持仓显示 | 待开发 |
| 基础 UI 框架 | 待开发 |

### 9.2 v1.1

| 功能 | 状态 |
|-----|------|
| 持有者集中度分析 | 待开发 |
| 市场数据整合 | 待开发 |
| 简化版风险评分 | 待开发 |

### 9.3 v1.2

| 功能 | 状态 |
|-----|------|
| Creator BNB 余额 | 待开发 |
| 社交链接展示 | 待开发 |
| 持有者列表展示 | 待开发 |
| UI 优化 | 待开发 |

---

## 附录：API 速查表

### Four.meme API

```http
# 获取代币详情 + Creator
GET https://four.meme/meme-api/v1/private/token/get?address={tokenAddress}

# 获取持有者列表 (Top 100)
GET https://four.meme/meme-api/v1/private/token/holder?address={tokenAddress}&pageNo=1&pageSize=100
```

### DexScreener API

```http
# 获取代币交易对
GET https://api.dexscreener.com/latest/dex/tokens/{tokenAddress}

# 获取单个交易对
GET https://api.dexscreener.com/latest/dex/pairs/bsc/{pairAddress}
```

### GeckoTerminal API

```http
# 获取代币信息
GET https://api.geckoterminal.com/api/v2/networks/bsc/tokens/{tokenAddress}

# 获取代币流动性池
GET https://api.geckoterminal.com/api/v2/networks/bsc/tokens/{tokenAddress}/pools
```

### BSC RPC

```javascript
// 获取 BNB 余额
await provider.getBalance(address)

// 获取代币余额
await provider.call({
  to: tokenAddress,
  data: encodeFunctionData('balanceOf', [address])
})
```

---

**文档结束**
