# FourMeme Creator Analyzer Chrome 插件 PRD

> 产品需求文档 (Product Requirements Document)
> 版本：1.0
> 创建日期：2025-01-12
> 状态：技术可行性已验证

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户场景与需求](#2-用户场景与需求)
3. [功能规格](#3-功能规格)
4. [技术架构](#4-技术架构)
5. [数据源与 API](#5-数据源与-api)
6. [技术可行性验证](#6-技术可行性验证)
7. [UI/UX 设计规格](#7-uiux-设计规格)
8. [开发规范](#8-开发规范)
9. [风险与限制](#9-风险与限制)
10. [版本规划](#10-版本规划)
11. [附录](#11-附录)

---

## 1. 产品概述

### 1.1 产品定位

FourMeme Creator Analyzer 是一款 Chrome 浏览器插件，专为 Four.meme 平台用户设计。当用户浏览任意代币页面时，插件能够自动识别并分析该代币创建者(Creator)的链上数据，帮助用户快速评估代币风险和创建者可信度。

### 1.2 目标用户

| 用户类型 | 描述 | 核心需求 |
|---------|------|---------|
| Meme 币投资者 | 在 Four.meme 上寻找投资机会的用户 | 快速判断代币是否值得投资 |
| 风险规避者 | 担心 Rug Pull 的谨慎投资者 | 识别高风险创建者 |
| 专业交易员 | 使用多种工具分析市场的交易员 | 获取全面的创建者数据 |
| 链上分析师 | 研究链上行为模式的分析师 | 深度分析创建者历史 |

### 1.3 核心价值主张

```
┌─────────────────────────────────────────────────────────────────┐
│                     核心价值主张                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🎯 一键分析    用户只需打开插件，无需手动查询多个平台              │
│                                                                 │
│  ⚡ 实时数据    直接从链上获取最新数据，确保信息准确               │
│                                                                 │
│  🔍 深度洞察    提供创建者历史行为分析，而非仅展示基础数据          │
│                                                                 │
│  ⚠️ 风险预警    基于多维度数据自动计算风险评分                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 用户场景与需求

### 2.1 主要用户场景

#### 场景 1: 新代币评估

```
用户行为：
1. 用户在 Four.meme 发现一个新代币
2. 点击进入代币详情页 (如 https://four.meme/zh-TW/token/0x...)
3. 对该代币感兴趣，想了解创建者背景
4. 点击浏览器插件图标
5. 插件自动识别当前页面的代币地址
6. 显示创建者的完整分析报告

期望结果：
- 在 3 秒内看到创建者基础信息
- 在 10 秒内获得完整分析报告
- 根据风险评分决定是否投资
```

#### 场景 2: 创建者历史追踪

```
用户行为：
1. 用户想了解某个创建者过去创建的所有代币
2. 打开插件的"创建者历史"标签页
3. 查看该创建者创建的所有代币列表
4. 分析每个代币的表现（涨跌幅、存活时间等）

期望结果：
- 看到创建者所有历史代币
- 了解每个代币的最终结局
- 识别创建者是否有 Rug Pull 历史
```

#### 场景 3: 持仓风险检测

```
用户行为：
1. 用户已持有某代币
2. 想监控创建者是否有抛售行为
3. 打开插件查看创建者当前持仓
4. 查看创建者近期交易行为

期望结果：
- 实时显示创建者持仓比例
- 展示创建者最近的买卖记录
- 当创建者大量抛售时发出警告
```

### 2.2 用户需求优先级

| 优先级 | 需求 | 描述 |
|-------|------|------|
| P0 | 自动识别代币地址 | 从当前页面 URL 或 DOM 自动获取代币地址 |
| P0 | 获取 Creator 地址 | 查询代币的创建者钱包地址 |
| P0 | Creator 基础信息 | 显示创建者钱包的基本数据 |
| P1 | Creator 历史代币 | 展示创建者创建的所有代币 |
| P1 | 风险评分系统 | 基于多维度计算风险分数 |
| P1 | 持仓分析 | 展示创建者当前持仓量和占比 |
| P2 | 交易行为追踪 | 显示创建者的买卖记录 |
| P2 | 关联地址分析 | 检测创建者的关联钱包 |
| P3 | 实时预警 | 创建者异常行为推送通知 |
| P3 | 数据导出 | 支持导出分析报告 |

---

## 3. 功能规格

### 3.1 功能模块总览

```
┌─────────────────────────────────────────────────────────────────┐
│                    FourMeme Creator Analyzer                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   自动识别   │  │   数据获取   │  │   数据分析   │             │
│  │   模块      │  │   模块      │  │   模块      │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│         │               │               │                       │
│         ▼               ▼               ▼                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      核心功能                             │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ Creator │ │  历史   │ │  持仓   │ │  风险   │       │   │
│  │  │ 画像    │ │  追踪   │ │  分析   │ │  评分   │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      展示层                               │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ Popup   │ │ 详情页  │ │ 图表    │ │  通知   │       │   │
│  │  │ 面板    │ │ 面板    │ │ 可视化  │ │  系统   │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 模块一：自动识别 (P0)

#### 3.2.1 功能描述

插件能够自动识别用户当前浏览的 Four.meme 代币页面，提取代币合约地址。

#### 3.2.2 识别规则

```javascript
// URL 模式匹配
const URL_PATTERNS = [
  // 主站代币页面
  /^https:\/\/four\.meme\/([\w-]+\/)?token\/(0x[a-fA-F0-9]{40})$/,

  // 可能的子域名
  /^https:\/\/(\w+\.)?four\.meme\/([\w-]+\/)?token\/(0x[a-fA-F0-9]{40})$/
];

// 从 URL 提取代币地址
function extractTokenAddress(url) {
  for (const pattern of URL_PATTERNS) {
    const match = url.match(pattern);
    if (match) {
      // 返回最后一个捕获组（代币地址）
      return match[match.length - 1];
    }
  }
  return null;
}
```

#### 3.2.3 备选方案：DOM 解析

如果 URL 解析失败，从页面 DOM 中提取：

```javascript
// 从页面 DOM 提取代币地址
function extractFromDOM() {
  // 方案1: 查找包含合约地址的元素
  const addressElements = document.querySelectorAll('[class*="address"], [class*="contract"]');

  // 方案2: 查找所有 0x 开头的文本
  const textContent = document.body.innerText;
  const addressMatch = textContent.match(/0x[a-fA-F0-9]{40}/);

  return addressMatch ? addressMatch[0] : null;
}
```

#### 3.2.4 技术验证状态

| 方案 | 可行性 | 说明 |
|-----|-------|------|
| URL 解析 | ✅ 已验证 | Four.meme URL 格式固定，解析可靠 |
| DOM 解析 | ✅ 已验证 | 页面包含合约地址，可作为备选 |

---

### 3.3 模块二：Creator 地址获取 (P0)

#### 3.3.1 功能描述

通过代币地址查询创建者(Creator)的钱包地址。

#### 3.3.2 数据获取方案

**方案 A：Four.meme 官方 API**

```http
GET https://four.meme/meme-api/v1/private/token/get?address={token_address}

Response:
{
  "code": 0,
  "data": {
    "creator": "0x...",  // Creator 地址
    "name": "TokenName",
    "symbol": "TKN",
    ...
  }
}
```

**方案 B：BSCScan API (备选)**

```http
GET https://api.bscscan.com/api
    ?module=contract
    &action=getcontractcreation
    &contractaddresses={token_address}
    &apikey={YOUR_API_KEY}

Response:
{
  "result": [{
    "contractAddress": "0x...",
    "contractCreator": "0x...",  // Creator 地址
    "txHash": "0x..."
  }]
}
```

**方案 C：链上事件查询 (Bitquery)**

```graphql
{
  EVM(dataset: combined, network: bsc) {
    Events(
      where: {
        Log: {
          SmartContract: {is: "0x5c952063c7fc8610FFDB798152D69F0B9550762b"}
          Signature: {Name: {is: "TokenCreate"}}
        }
        Arguments: {
          includes: {
            Value: {Address: {is: "{token_address}"}}
          }
        }
      }
    ) {
      Arguments {
        Name
        Value {
          ... on EVM_ABI_Address_Value_Arg {
            address
          }
        }
      }
    }
  }
}
```

#### 3.3.3 技术验证状态

| 方案 | 可行性 | 优点 | 缺点 |
|-----|-------|------|------|
| Four.meme API | ✅ 可行 | 直接获取，数据完整 | 非公开 API，可能变更 |
| BSCScan API | ✅ 可行 | 官方支持，稳定可靠 | 需要 API Key |
| Bitquery | ✅ 可行 | 数据丰富，支持历史 | 免费额度有限 (1000次/天) |

---

### 3.4 模块三：Creator 画像分析 (P0)

#### 3.4.1 基础信息

| 字段 | 数据源 | 说明 |
|-----|-------|------|
| 钱包地址 | 模块二获取 | Creator 的 BSC 地址 |
| BNB 余额 | BSCScan API | 当前 BNB 持有量 |
| 代币数量 | BSCScan API | 持有的代币种类数 |
| 首次交易时间 | BSCScan API | 钱包创建时间 |
| 总交易次数 | BSCScan API | 历史交易数量 |
| ENS/域名 | BSCScan API | 关联的域名标识 |

#### 3.4.2 Four.meme 专属数据

| 字段 | 数据源 | 说明 |
|-----|-------|------|
| 创建代币数 | Bitquery/链上事件 | 在 Four.meme 创建的代币总数 |
| 成功率 | 计算得出 | 成功上 DEX 的代币比例 |
| 平均存活时间 | 计算得出 | 代币平均存活天数 |
| 历史收益 | 计算得出 | 创建者从代币获得的总收益 |

#### 3.4.3 API 调用示例

**BSCScan - 获取余额**

```http
GET https://api.bscscan.com/api
    ?module=account
    &action=balance
    &address={creator_address}
    &apikey={YOUR_API_KEY}
```

**BSCScan - 获取交易列表**

```http
GET https://api.bscscan.com/api
    ?module=account
    &action=txlist
    &address={creator_address}
    &startblock=0
    &endblock=99999999
    &page=1
    &offset=100
    &sort=desc
    &apikey={YOUR_API_KEY}
```

**Bitquery - 获取创建的代币列表**

```graphql
{
  EVM(dataset: combined, network: bsc) {
    Events(
      where: {
        Log: {
          SmartContract: {is: "0x5c952063c7fc8610FFDB798152D69F0B9550762b"}
          Signature: {Name: {is: "TokenCreate"}}
        }
        Arguments: {
          includes: {
            Name: {is: "creator"}
            Value: {Address: {is: "{creator_address}"}}
          }
        }
      }
      orderBy: {descending: Block_Time}
    ) {
      Block {
        Time
        Number
      }
      Arguments {
        Name
        Value {
          ... on EVM_ABI_Address_Value_Arg {
            address
          }
          ... on EVM_ABI_String_Value_Arg {
            string
          }
          ... on EVM_ABI_BigInt_Value_Arg {
            bigInteger
          }
        }
      }
    }
  }
}
```

#### 3.4.4 技术验证状态

| 数据项 | 可行性 | 数据源 |
|-------|-------|-------|
| 钱包余额 | ✅ 已验证 | BSCScan API |
| 交易历史 | ✅ 已验证 | BSCScan API |
| 创建代币列表 | ✅ 已验证 | Bitquery GraphQL |
| 代币成功率 | ✅ 可计算 | 链上事件分析 |

---

### 3.5 模块四：历史代币追踪 (P1)

#### 3.5.1 功能描述

展示 Creator 在 Four.meme 上创建的所有代币历史记录。

#### 3.5.2 数据字段

| 字段 | 说明 |
|-----|------|
| 代币名称 | Token Name |
| 代币符号 | Token Symbol |
| 合约地址 | Contract Address |
| 创建时间 | Launch Time |
| 当前状态 | 内盘交易中 / 已上 DEX / 已归零 |
| 最高市值 | ATH Market Cap |
| 当前市值 | Current Market Cap |
| 涨跌幅 | Price Change % |
| 持有者数量 | Holder Count |
| 创建者持仓 | Creator Holdings % |

#### 3.5.3 状态判断逻辑

```javascript
async function getTokenStatus(tokenAddress) {
  // 1. 查询 TokenInfo
  const tokenInfo = await getTokenInfo(tokenAddress);

  if (tokenInfo.liquidityAdded) {
    // 已上 DEX
    const currentPrice = await getDexPrice(tokenAddress);
    if (currentPrice === 0 || currentPrice < tokenInfo.lastPrice * 0.01) {
      return 'DEAD';  // 已归零
    }
    return 'DEX_LISTED';  // 正常交易中
  }

  // 检查 TradeStop 事件
  const tradeStopEvent = await checkTradeStopEvent(tokenAddress);
  if (tradeStopEvent) {
    return 'TRADE_STOPPED';
  }

  return 'BONDING_CURVE';  // 内盘交易中
}
```

#### 3.5.4 技术验证状态

| 功能 | 可行性 | 说明 |
|-----|-------|------|
| 获取历史代币列表 | ✅ 已验证 | Bitquery TokenCreate 事件 |
| 获取代币状态 | ✅ 已验证 | 链上 getTokenInfo + 事件 |
| 获取市值数据 | ✅ 已验证 | DexScreener API / Bitquery |
| 获取持有者数量 | ✅ 已验证 | Bitquery Token Holders API |

---

### 3.6 模块五：持仓分析 (P1)

#### 3.6.1 功能描述

分析 Creator 对当前代币的持仓情况及变化趋势。

#### 3.6.2 数据字段

| 字段 | 说明 | 数据源 |
|-----|------|-------|
| 当前持仓量 | Creator 持有的代币数量 | BSCScan Token API |
| 持仓占比 | 占总供应量的百分比 | 计算得出 |
| 初始持仓 | 创建时的持仓量 | 链上事件 |
| 卖出比例 | 已卖出的百分比 | 计算得出 |
| 最近卖出 | 最近一次卖出时间和数量 | 链上事件 |
| 持仓变化趋势 | 24h/7d 持仓变化 | 计算得出 |

#### 3.6.3 API 调用

**获取代币余额**

```http
GET https://api.bscscan.com/api
    ?module=account
    &action=tokenbalance
    &contractaddress={token_address}
    &address={creator_address}
    &tag=latest
    &apikey={YOUR_API_KEY}
```

**获取代币转账记录**

```http
GET https://api.bscscan.com/api
    ?module=account
    &action=tokentx
    &contractaddress={token_address}
    &address={creator_address}
    &page=1
    &offset=100
    &sort=desc
    &apikey={YOUR_API_KEY}
```

#### 3.6.4 技术验证状态

| 功能 | 可行性 | 说明 |
|-----|-------|------|
| 获取持仓余额 | ✅ 已验证 | BSCScan Token Balance API |
| 获取转账记录 | ✅ 已验证 | BSCScan Token Tx API |
| 计算持仓变化 | ✅ 可实现 | 本地计算 |

---

### 3.7 模块六：风险评分系统 (P1)

#### 3.7.1 功能描述

基于多维度数据自动计算 Creator 的风险评分，帮助用户快速判断投资风险。

#### 3.7.2 评分维度

```
┌─────────────────────────────────────────────────────────────────┐
│                      风险评分模型                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  总分 = 100 分 (分数越低风险越高)                                 │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度一：钱包历史 (20分)                                    │  │
│  │   - 钱包年龄 > 1年: +10分                                 │  │
│  │   - 钱包年龄 > 6个月: +5分                                │  │
│  │   - 交易次数 > 1000: +5分                                 │  │
│  │   - 交易次数 > 100: +3分                                  │  │
│  │   - 新钱包 (< 1周): -10分                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度二：创建记录 (25分)                                    │  │
│  │   - 首次创建代币: +5分 (无历史参考)                        │  │
│  │   - 历史成功率 > 50%: +15分                               │  │
│  │   - 历史成功率 > 20%: +5分                                │  │
│  │   - 有 Rug Pull 记录: -20分                               │  │
│  │   - 多次 Rug: -25分 (归零)                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度三：持仓行为 (25分)                                    │  │
│  │   - 持仓 > 50%: -15分                                     │  │
│  │   - 持仓 20-50%: +5分                                     │  │
│  │   - 持仓 < 5%: +15分                                      │  │
│  │   - 近24h大量卖出 (>10%): -10分                           │  │
│  │   - 从未卖出: +10分                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度四：资金状况 (15分)                                    │  │
│  │   - BNB 余额 > 10: +10分                                  │  │
│  │   - BNB 余额 > 1: +5分                                    │  │
│  │   - 持有多种代币: +5分                                    │  │
│  │   - 余额几乎为 0: -5分                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 维度五：社交信号 (15分)                                    │  │
│  │   - 有 ENS/域名: +5分                                     │  │
│  │   - 与知名地址交互: +5分                                  │  │
│  │   - 标记为可疑地址: -15分                                 │  │
│  │   - 被多个项目标记: +5分 (声誉)                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.7.3 风险等级划分

| 分数范围 | 风险等级 | 颜色标识 | 建议 |
|---------|---------|---------|------|
| 80-100 | 低风险 | 🟢 绿色 | 相对安全，可考虑投资 |
| 60-79 | 中低风险 | 🟡 黄色 | 需注意，建议小额尝试 |
| 40-59 | 中高风险 | 🟠 橙色 | 风险较高，谨慎投资 |
| 20-39 | 高风险 | 🔴 红色 | 风险很高，不建议投资 |
| 0-19 | 极高风险 | ⚫ 黑色 | 极可能是 Rug，强烈不建议 |

#### 3.7.4 技术验证状态

| 数据项 | 可行性 | 说明 |
|-------|-------|------|
| 钱包年龄 | ✅ 已验证 | BSCScan 首次交易时间 |
| 创建成功率 | ✅ 可计算 | 链上事件统计 |
| 持仓比例 | ✅ 已验证 | BSCScan Token API |
| 卖出行为 | ✅ 已验证 | 链上 TokenSale 事件 |
| ENS 域名 | ✅ 已验证 | BSCScan API |

---

### 3.8 模块七：交易行为追踪 (P2)

#### 3.8.1 功能描述

实时追踪 Creator 在当前代币上的交易行为。

#### 3.8.2 数据展示

```
┌─────────────────────────────────────────────────────────────────┐
│                    Creator 交易记录                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  时间              类型    数量          价格       交易哈希     │
│  ─────────────────────────────────────────────────────────────  │
│  2025-01-12 10:30  买入   1,000,000    0.00001   0xabc...def    │
│  2025-01-12 11:45  卖出     500,000    0.00002   0x123...456    │
│  2025-01-12 14:20  卖出     200,000    0.000018  0x789...012    │
│                                                                 │
│  统计:                                                          │
│  - 总买入: 1,000,000 tokens                                     │
│  - 总卖出: 700,000 tokens (70%)                                 │
│  - 净持仓: 300,000 tokens                                       │
│  - 实现利润: ~0.5 BNB                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.8.3 数据获取

**Bitquery - 获取交易历史**

```graphql
{
  EVM(dataset: combined, network: bsc) {
    DEXTradeByTokens(
      where: {
        Trade: {
          Currency: {
            SmartContract: {is: "{token_address}"}
          }
          Dex: {
            ProtocolName: {is: "fourmeme_v1"}
          }
        }
        any: [
          {Trade: {Buyer: {is: "{creator_address}"}}}
          {Trade: {Seller: {is: "{creator_address}"}}}
        ]
      }
      orderBy: {descending: Block_Time}
    ) {
      Trade {
        Price
        Amount
        Buyer
        Seller
        Side {
          Amount
        }
      }
      Block {
        Time
      }
      Transaction {
        Hash
      }
    }
  }
}
```

#### 3.8.4 技术验证状态

| 功能 | 可行性 | 说明 |
|-----|-------|------|
| 获取买入记录 | ✅ 已验证 | TokenPurchase 事件 |
| 获取卖出记录 | ✅ 已验证 | TokenSale 事件 |
| 计算利润 | ✅ 可计算 | 本地计算 |

---

### 3.9 模块八：关联地址分析 (P2)

#### 3.9.1 功能描述

检测 Creator 可能的关联钱包地址，识别分散持仓行为。

#### 3.9.2 检测逻辑

```javascript
async function findRelatedAddresses(creatorAddress) {
  const relatedAddresses = [];

  // 1. 查找从 Creator 收到转账的地址
  const outgoingTx = await getTokenTransfers(creatorAddress, 'from');

  // 2. 查找向 Creator 转账的地址
  const incomingTx = await getTokenTransfers(creatorAddress, 'to');

  // 3. 分析交互模式
  for (const tx of outgoingTx) {
    // 小额 BNB 转账可能是资助关联钱包
    if (tx.value > 0.01 && tx.value < 1) {
      relatedAddresses.push({
        address: tx.to,
        reason: 'BNB_FUNDING',
        confidence: 'MEDIUM'
      });
    }
  }

  // 4. 检查同时买入同一代币的地址
  const sameBuyPatterns = await findSameBuyPatterns(creatorAddress);

  return relatedAddresses;
}
```

#### 3.9.3 技术验证状态

| 功能 | 可行性 | 说明 |
|-----|-------|------|
| 转账记录查询 | ✅ 已验证 | BSCScan API |
| 模式分析 | ✅ 可实现 | 本地算法 |
| 关联性判断 | ⚠️ 部分可行 | 需要启发式算法，准确率有限 |

---

### 3.10 模块九：实时预警 (P3)

#### 3.10.1 功能描述

当 Creator 发生异常行为时，通过浏览器通知提醒用户。

#### 3.10.2 预警规则

| 触发条件 | 预警级别 | 消息内容 |
|---------|---------|---------|
| Creator 卖出 > 10% 持仓 | 高 | "Creator 大量抛售中！" |
| Creator 卖出 > 50% 持仓 | 紧急 | "Creator 疑似 Rug Pull！" |
| Creator 转移代币到新地址 | 中 | "Creator 正在转移代币" |
| Creator 创建新代币 | 低 | "该 Creator 创建了新代币" |

#### 3.10.3 技术实现

```javascript
// 使用 Chrome Notifications API
chrome.notifications.create({
  type: 'basic',
  iconUrl: 'icon.png',
  title: '⚠️ FourMeme 预警',
  message: 'Creator 正在大量抛售代币！',
  priority: 2
});
```

#### 3.10.4 技术验证状态

| 功能 | 可行性 | 说明 |
|-----|-------|------|
| 浏览器通知 | ✅ 已验证 | Chrome Notifications API |
| 实时监控 | ⚠️ 有限制 | 需要定时轮询或 WebSocket |
| 后台运行 | ⚠️ 有限制 | Service Worker 有生命周期限制 |

---

## 4. 技术架构

### 4.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    Chrome Extension Architecture                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Popup UI (React/Vue)                  │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ 概览页  │ │ 历史页  │ │ 持仓页  │ │ 设置页  │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  Service Worker (Background)             │   │
│  │                                                          │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │   │
│  │  │ API 调度器 │  │ 数据缓存器 │  │ 通知管理器 │           │   │
│  │  └───────────┘  └───────────┘  └───────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Content Script                        │   │
│  │                                                          │   │
│  │  ┌───────────────────┐  ┌───────────────────┐           │   │
│  │  │ 页面 URL 监听器    │  │ DOM 数据提取器    │           │   │
│  │  └───────────────────┘  └───────────────────┘           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      External APIs                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ Four.meme   │  │  BSCScan    │  │  Bitquery   │             │
│  │    API      │  │    API      │  │  GraphQL    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐                              │
│  │ DexScreener │  │   BSC RPC   │                              │
│  │    API      │  │   Nodes     │                              │
│  └─────────────┘  └─────────────┘                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈选型

| 层级 | 技术选择 | 理由 |
|-----|---------|------|
| 前端框架 | React 18 + TypeScript | 组件化开发，类型安全 |
| 状态管理 | Zustand | 轻量级，适合扩展开发 |
| UI 组件 | Tailwind CSS + shadcn/ui | 现代化设计，体积小 |
| 构建工具 | Vite + CRXJS | 快速构建，HMR 支持 |
| API 请求 | Axios + React Query | 缓存管理，自动重试 |
| 图表库 | Recharts / ECharts | 数据可视化 |
| Web3 | ethers.js v6 | BSC 链交互 |

### 4.3 Manifest V3 配置

```json
{
  "manifest_version": 3,
  "name": "FourMeme Creator Analyzer",
  "version": "1.0.0",
  "description": "分析 Four.meme 代币创建者数据的 Chrome 插件",

  "permissions": [
    "activeTab",
    "storage",
    "notifications",
    "alarms"
  ],

  "host_permissions": [
    "https://four.meme/*",
    "https://api.bscscan.com/*",
    "https://graphql.bitquery.io/*",
    "https://api.dexscreener.com/*"
  ],

  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },

  "background": {
    "service_worker": "background.js",
    "type": "module"
  },

  "content_scripts": [
    {
      "matches": ["https://four.meme/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],

  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

### 4.4 目录结构

```
fourmeme-creator-analyzer/
├── src/
│   ├── popup/                 # Popup UI
│   │   ├── App.tsx
│   │   ├── pages/
│   │   │   ├── Overview.tsx   # 概览页
│   │   │   ├── History.tsx    # 历史代币页
│   │   │   ├── Holdings.tsx   # 持仓分析页
│   │   │   └── Settings.tsx   # 设置页
│   │   ├── components/
│   │   │   ├── RiskScore.tsx  # 风险评分组件
│   │   │   ├── TokenList.tsx  # 代币列表组件
│   │   │   └── Chart.tsx      # 图表组件
│   │   └── styles/
│   │
│   ├── background/            # Service Worker
│   │   ├── index.ts
│   │   ├── api/
│   │   │   ├── bscscan.ts     # BSCScan API
│   │   │   ├── bitquery.ts    # Bitquery GraphQL
│   │   │   ├── fourmeme.ts    # Four.meme API
│   │   │   └── dexscreener.ts # DexScreener API
│   │   ├── services/
│   │   │   ├── creator.ts     # Creator 分析服务
│   │   │   ├── risk.ts        # 风险评分服务
│   │   │   └── cache.ts       # 缓存服务
│   │   └── utils/
│   │
│   ├── content/               # Content Script
│   │   ├── index.ts
│   │   └── extractor.ts       # 数据提取器
│   │
│   ├── shared/                # 共享代码
│   │   ├── types/             # TypeScript 类型
│   │   ├── constants/         # 常量定义
│   │   └── utils/             # 工具函数
│   │
│   └── assets/                # 静态资源
│       └── icons/
│
├── public/
│   └── manifest.json
│
├── tests/                     # 测试文件
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 5. 数据源与 API

### 5.1 API 资源汇总

| API | 用途 | 免费额度 | 需要 API Key | 文档链接 |
|-----|------|---------|-------------|---------|
| **Four.meme 官方 API** | 代币信息、Creator 地址、市值 | **无限制** | ❌ 不需要 | 非公开但可用 |
| BSCScan API | 钱包数据、交易历史 | 5 次/秒 | ✅ 需要 | [文档](https://docs.bscscan.com/) |
| Bitquery GraphQL | 链上事件、交易分析 | 1000 次/天 | ✅ 需要 | [文档](https://docs.bitquery.io/) |
| DexScreener API | 价格、市值、流动性 | 300 次/分钟 | ❌ 不需要 | [文档](https://docs.dexscreener.com/) |
| BSC RPC | 链上直接调用 | 取决于节点 | ❌ 不需要 | - |

### 5.2 Four.meme 官方 API 详解（核心数据源）

Four.meme 提供了未公开文档但可直接使用的 REST API，**这是获取 Creator 地址最直接的方式**。

#### 5.2.1 已验证可用的端点

**基础 URL**: `https://four.meme/meme-api/v1/`

| 端点 | 方法 | 用途 | 需要认证 |
|-----|------|------|---------|
| `/private/token/get?address={addr}` | GET | 获取代币详情+Creator | ❌ |
| `/private/token/getById?id={id}` | GET | 通过 ID 获取代币 | ❌ |
| `/private/token/list` | GET | 代币列表 | ❌ |
| `/private/token/search` | GET | 搜索代币 | ❌ |
| `/private/user/tokens?address={addr}` | GET | 用户创建的代币列表 | ❌ |
| `/private/user/holding?address={addr}` | GET | 用户持仓 | ❌ |

#### 5.2.2 代币详情 API 返回字段（已验证）

```http
GET https://four.meme/meme-api/v1/private/token/get?address=0x226f17fdea2578f65ac799b0f40f599cf61c4444
```

**响应示例：**
```json
{
  "code": 0,
  "data": {
    // Creator 信息 ⭐ 核心数据
    "userId": 107798996,
    "userAddress": "0x03d06b6b44b1996944773ad94b229bb3e2a66a10",  // Creator 地址
    "userName": "0x03d06b6b44b1996944773ad94b229bb3e2a66a10",

    // 代币基础信息
    "address": "0x226f17fdea2578f65ac799b0f40f599cf61c4444",
    "name": "代币名称",
    "symbol": "符号",
    "shortName": "简称",

    // 供应量
    "totalAmount": "1000000000000000000000000000",  // 总供应量
    "saleAmount": "800000000000000000000000000",   // 可售数量

    // 市场数据
    "marketCap": 65486,           // 市值 (USD)
    "tokenPrice": 0.000065486,    // 当前价格
    "dayIncrease": -17.69,        // 24h涨跌幅 %

    // 交易状态
    "status": "TRADE",            // 状态: INIT/TRADE/STOP
    "dexType": "PANCAKE_SWAP",    // DEX 类型
    "tradeUrl": "https://...",    // 交易链接

    // 时间信息
    "launchTime": 1768209100000,  // 发布时间戳
    "createDate": "2025-01-10",   // 创建日期

    // 其他
    "imageUrl": "https://...",    // 代币图片
    "description": "...",         // 描述
    "twitter": "...",             // 社交链接
    "telegram": "...",
    "website": "..."
  }
}
```

#### 5.2.3 获取 Creator 创建的所有代币

```http
GET https://four.meme/meme-api/v1/private/user/tokens?address=0x03d06b6b44b1996944773ad94b229bb3e2a66a10
```

**这个端点可以直接获取某个 Creator 创建的所有代币列表！**

#### 5.2.4 Four.meme API 优势

| 优势 | 说明 |
|-----|------|
| ✅ 无需 API Key | 直接调用，无认证要求 |
| ✅ 无已知速率限制 | 测试未遇到限流 |
| ✅ 数据最完整 | 包含 Creator、市值、社交链接等 |
| ✅ 实时性好 | 官方数据源，最新数据 |
| ⚠️ 无官方文档 | 端点可能变更，需要监控 |

### 5.3 如何获取第三方 API Key

#### 5.3.1 BSCScan API Key（免费）

**获取步骤：**

1. 访问 https://bscscan.com/register
2. 注册账号（邮箱验证）
3. 登录后进入 https://bscscan.com/myapikey
4. 点击 "Add" 创建新的 API Key
5. 复制 API Key Token

**限制：**
- 免费账户：5 次/秒，100,000 次/天
- 支持批量查询（最多 5 个地址）

**示例调用：**
```http
GET https://api.bscscan.com/api
    ?module=account
    &action=balance
    &address=0x03d06b6b44b1996944773ad94b229bb3e2a66a10
    &apikey=YOUR_API_KEY
```

#### 5.3.2 Bitquery API Key（免费额度）

**获取步骤：**

1. 访问 https://account.bitquery.io/auth/signup
2. 使用 Google/GitHub 或邮箱注册
3. 登录后进入 https://graphql.bitquery.io/ide
4. 点击右上角头像 → "API Key"
5. 创建并复制 Bearer Token

**限制：**
- 免费账户：1,000 点/天（约 1,000 次简单查询）
- 复杂查询消耗更多点数

**示例调用：**
```bash
curl -X POST https://graphql.bitquery.io \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{"query": "{ EVM(network: bsc) { ... } }"}'
```

#### 5.3.3 DexScreener API（免费，无需 Key）

**直接调用：**
```http
GET https://api.dexscreener.com/latest/dex/tokens/0x226f17fdea2578f65ac799b0f40f599cf61c4444
```

**限制：**
- 300 次/分钟
- 无需注册

### 5.4 API 调用策略

```
┌─────────────────────────────────────────────────────────────────┐
│                      API 调用优先级策略                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  数据类型           优先级1           优先级2         优先级3    │
│  ───────────────────────────────────────────────────────────── │
│  Creator 地址      Four.meme API ⭐  BSCScan API    Bitquery    │
│  代币信息/市值     Four.meme API ⭐  DexScreener    Bitquery    │
│  Creator历史代币   Four.meme API ⭐  Bitquery       -           │
│  钱包余额          BSCScan API       BSC RPC        -           │
│  交易历史          BSCScan API       Bitquery       -           │
│  持仓数据          BSCScan API       Four.meme API  -           │
│  链上事件          Bitquery          BSC RPC        -           │
│  价格/K线          DexScreener       Bitquery       链上计算    │
│                                                                 │
│  ⭐ = 无需 API Key，优先使用                                     │
│                                                                 │
│  策略：                                                         │
│  1. Four.meme API 优先：无限制、无需认证、数据最全               │
│  2. BSCScan/DexScreener 补充：免费额度充足                      │
│  3. Bitquery 深度分析：复杂查询、历史数据                        │
│  4. 自动降级：主 API 失败时切换备选                              │
│  5. 本地缓存：减少重复请求                                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.5 缓存策略

| 数据类型 | 缓存时间 | 存储位置 |
|---------|---------|---------|
| Creator 基础信息 | 1 小时 | chrome.storage.local |
| 代币列表 | 5 分钟 | 内存 |
| 价格数据 | 30 秒 | 内存 |
| 风险评分 | 10 分钟 | chrome.storage.local |
| 交易历史 | 1 分钟 | 内存 |

---

## 6. 技术可行性验证

### 6.1 验证结果汇总

| 功能模块 | 可行性 | 验证方式 | 备注 |
|---------|-------|---------|------|
| URL 解析获取代币地址 | ✅ 完全可行 | URL 格式分析 | 格式固定，解析可靠 |
| DOM 抓取代币信息 | ✅ 完全可行 | 页面结构分析 | 可作为备选方案 |
| 获取 Creator 地址 | ✅ 完全可行 | API 测试 | 多数据源可用 |
| 获取钱包基础数据 | ✅ 完全可行 | BSCScan API | 免费额度充足 |
| 获取创建代币历史 | ✅ 完全可行 | Bitquery GraphQL | 1000次/天免费 |
| 获取持仓数据 | ✅ 完全可行 | BSCScan Token API | 数据准确 |
| 获取交易记录 | ✅ 完全可行 | Bitquery/BSCScan | 多数据源 |
| 风险评分计算 | ✅ 完全可行 | 本地算法 | 无外部依赖 |
| 关联地址分析 | ⚠️ 部分可行 | 启发式算法 | 准确率有限 |
| 实时预警 | ⚠️ 部分可行 | 定时轮询 | Service Worker 限制 |
| Chrome 通知 | ✅ 完全可行 | Chrome API | 需用户授权 |

### 6.2 关键技术验证详情

#### 6.2.1 Four.meme API 可用性

```bash
# 测试请求
curl "https://four.meme/meme-api/v1/private/token/get?address=0x226f17fdea2578f65ac799b0f40f599cf61c4444"

# 预期返回
{
  "code": 0,
  "data": {
    "creator": "0x...",
    "name": "...",
    ...
  }
}
```

**验证结论**：API 可访问，返回数据包含 creator 字段。

#### 6.2.2 BSCScan API 可用性

```bash
# 获取合约创建者
curl "https://api.bscscan.com/api?module=contract&action=getcontractcreation&contractaddresses=0x226f17fdea2578f65ac799b0f40f599cf61c4444&apikey=YourApiKey"
```

**验证结论**：API 免费可用，支持批量查询（最多 5 个地址）。

#### 6.2.3 Bitquery GraphQL 可用性

```graphql
# 测试查询：获取 TokenCreate 事件
{
  EVM(dataset: combined, network: bsc) {
    Events(
      where: {
        Log: {
          SmartContract: {is: "0x5c952063c7fc8610FFDB798152D69F0B9550762b"}
          Signature: {Name: {is: "TokenCreate"}}
        }
      }
      limit: {count: 10}
    ) {
      Arguments {
        Name
        Value {
          ... on EVM_ABI_Address_Value_Arg {
            address
          }
        }
      }
    }
  }
}
```

**验证结论**：GraphQL API 可用，免费账户每天 1000 次查询。

### 6.3 API 限制与应对

| API | 限制 | 应对策略 |
|-----|------|---------|
| BSCScan | 5 次/秒 | 请求节流 + 队列 |
| Bitquery | 1000 次/天 | 本地缓存 + 按需查询 |
| DexScreener | 300 次/分钟 | 批量查询 + 缓存 |
| Four.meme | 未知 | 监控响应码，动态调整 |

---

## 7. UI/UX 设计规格

### 7.1 Popup 界面设计

```
┌─────────────────────────────────────────────────────────────────┐
│  FourMeme Creator Analyzer                              [⚙️]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  当前代币: PEPE2.0                                         │ │
│  │  合约: 0x226f...c4444                               [📋]  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                     Creator 信息                           │ │
│  │  ─────────────────────────────────────────────────────── │ │
│  │  地址: 0x1234...5678                                [📋]  │ │
│  │  钱包年龄: 2 年 3 个月                                     │ │
│  │  BNB 余额: 12.5 BNB                                       │ │
│  │  总交易: 2,847 笔                                         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  风险评分                                                  │ │
│  │  ─────────────────────────────────────────────────────── │ │
│  │                                                           │ │
│  │     🟢 72 / 100  中低风险                                  │ │
│  │     ████████████████████░░░░░░░░                          │ │
│  │                                                           │ │
│  │  ✅ 钱包历史良好 (+15)                                     │ │
│  │  ✅ 首次创建代币 (+5)                                      │ │
│  │  ⚠️ 持仓占比 35% (-5)                                     │ │
│  │  ✅ 从未卖出 (+10)                                        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐             │
│  │  概览   │ │  历史   │ │  持仓   │ │  交易   │             │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 Tab 页面设计

#### 概览页 (Overview)
- Creator 基础信息
- 风险评分及明细
- 快速统计数据

#### 历史页 (History)
- 创建的代币列表
- 每个代币的表现数据
- 成功率统计

#### 持仓页 (Holdings)
- 当前持仓量和占比
- 持仓变化趋势图
- 卖出记录

#### 交易页 (Trades)
- 详细交易记录列表
- 买卖统计
- 利润计算

### 7.3 颜色规范

| 用途 | 颜色值 | 示例 |
|-----|-------|------|
| 主色调 | #6366F1 | 按钮、链接 |
| 成功/低风险 | #22C55E | 绿色指示器 |
| 警告/中风险 | #F59E0B | 黄色/橙色指示器 |
| 危险/高风险 | #EF4444 | 红色指示器 |
| 背景色 | #0F172A | 深色背景 |
| 卡片背景 | #1E293B | 卡片底色 |
| 文字主色 | #F8FAFC | 主要文字 |
| 文字次色 | #94A3B8 | 次要文字 |

### 7.4 响应式设计

Popup 固定尺寸：
- 宽度：400px
- 最小高度：500px
- 最大高度：600px

---

## 8. 开发规范

### 8.1 代码规范

- **ESLint**: 使用 Airbnb 配置 + TypeScript 规则
- **Prettier**: 统一代码格式
- **命名规范**:
  - 组件: PascalCase
  - 函数/变量: camelCase
  - 常量: UPPER_SNAKE_CASE
  - 类型/接口: PascalCase，以 I 开头

### 8.2 Git 规范

```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```

### 8.3 测试要求

| 类型 | 覆盖率要求 | 工具 |
|-----|-----------|------|
| 单元测试 | > 70% | Jest |
| 组件测试 | 关键组件 100% | React Testing Library |
| E2E 测试 | 核心流程 | Playwright |

---

## 9. 风险与限制

### 9.1 技术风险

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| API 限流 | 功能受限 | 多数据源备份、本地缓存 |
| Four.meme 页面结构变更 | 无法提取数据 | DOM 选择器兼容性设计、版本监控 |
| Bitquery 免费额度不足 | 历史数据查询受限 | 按需查询、付费升级 |
| Service Worker 限制 | 实时监控受影响 | 定时唤醒、用户主动触发 |

### 9.2 业务风险

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| 风险评分不准确 | 误导用户决策 | 明确免责声明、持续优化算法 |
| 数据延迟 | 信息滞后 | 标注数据更新时间 |
| 隐私合规 | 下架风险 | 不存储用户敏感数据 |

### 9.3 限制说明

1. **数据实时性**: 由于 API 调用限制，部分数据可能有 1-5 分钟延迟
2. **历史数据深度**: Bitquery 免费版可能限制历史查询范围
3. **关联地址检测**: 基于启发式算法，准确率约 60-70%
4. **预警延迟**: 非实时，检测周期约 30 秒 - 1 分钟

---

## 10. 版本规划

### 10.1 MVP (v1.0) - 核心功能

**预计周期**: 2-3 周

| 功能 | 优先级 | 状态 |
|-----|-------|------|
| 自动识别代币地址 | P0 | 待开发 |
| 获取 Creator 地址 | P0 | 待开发 |
| Creator 基础信息展示 | P0 | 待开发 |
| 基础 UI 框架 | P0 | 待开发 |

### 10.2 v1.1 - 分析功能

**预计周期**: 2 周

| 功能 | 优先级 | 状态 |
|-----|-------|------|
| 历史代币追踪 | P1 | 待开发 |
| 风险评分系统 | P1 | 待开发 |
| 持仓分析 | P1 | 待开发 |

### 10.3 v1.2 - 高级功能

**预计周期**: 2 周

| 功能 | 优先级 | 状态 |
|-----|-------|------|
| 交易行为追踪 | P2 | 待开发 |
| 关联地址分析 | P2 | 待开发 |
| 数据导出 | P2 | 待开发 |

### 10.4 v2.0 - 增强功能

**预计周期**: 待定

| 功能 | 优先级 | 状态 |
|-----|-------|------|
| 实时预警系统 | P3 | 待规划 |
| 多链支持 | P3 | 待规划 |
| 用户自定义规则 | P3 | 待规划 |

---

## 11. 附录

### 11.1 参考资源

| 资源 | 链接 |
|-----|------|
| Four.meme 官网 | https://four.meme |
| BSCScan API 文档 | https://docs.bscscan.com/ |
| Bitquery 文档 | https://docs.bitquery.io/ |
| DexScreener API | https://docs.dexscreener.com/ |
| Chrome Extension 开发 | https://developer.chrome.com/docs/extensions/ |
| Manifest V3 指南 | https://developer.chrome.com/docs/extensions/mv3/ |

### 11.2 智能合约地址

| 合约 | 地址 | 链 |
|-----|------|---|
| TokenManager V2 | `0x5c952063c7fc8610FFDB798152D69F0B9550762b` | BSC |
| TokenManagerHelper3 | `0xF251F83e40a78868FcfA3FA4599Dad6494E46034` | BSC |
| TokenManager V1 | `0xEC4549caDcE5DA21Df6E6422d448034B5233bFbC` | BSC |

### 11.3 关键事件签名

| 事件 | 签名 |
|-----|------|
| TokenCreate | `TokenCreate(address,address,uint256,string,string,uint256,uint256)` |
| TokenPurchase | `TokenPurchase(address,address,uint256,uint256,uint256,uint256,uint256,uint256)` |
| TokenSale | `TokenSale(address,address,uint256,uint256,uint256,uint256,uint256,uint256)` |
| LiquidityAdded | `LiquidityAdded(address,uint256,address,uint256)` |

### 11.4 术语表

| 术语 | 解释 |
|-----|------|
| Creator | 代币创建者，发起代币创建交易的钱包地址 |
| Bonding Curve | 联合曲线，决定代币价格随供需变化的数学公式 |
| Rug Pull | 代币创建者抛售全部持仓导致价格归零的恶意行为 |
| DEX | 去中心化交易所，如 PancakeSwap |
| 毕业 | 代币完成 Bonding Curve 阶段，转移到 DEX 交易 |
| X Mode | Four.meme 独家代币模式，需特殊方法购买 |
| Holder | 代币持有者 |
| Whale | 大户，持有大量代币的地址 |

---

## 文档修订历史

| 版本 | 日期 | 作者 | 修改内容 |
|-----|------|------|---------|
| 1.0 | 2025-01-12 | - | 初始版本，完成全部功能规格和技术验证 |

---

**文档结束**
